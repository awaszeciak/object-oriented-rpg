#pragma once
#include "RewardType.h"
#include "Item.h"
#include "Character.h"

class Reward {
    private:
        RewardType _type;
        int _goldValue;
        Character _character;
        Item _item;

    public:
        // explicit Reward(int gold);
        // explicit Reward();
        // explicit Reward();
        

};